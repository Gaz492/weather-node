<!-- Optional JavaScript -->
<!-- jQuery first, then Popper.js, then Bootstrap JS -->
<script src="https://code.jquery.com/jquery-3.2.1.slim.min.js"
        integrity="sha384-KJ3o2DKtIkvYIK3UENzmM7KCkRr/rE9/Qpg6aAZGJwFDMVNA/GpGFF93hXpG5KkN"
        crossorigin="anonymous"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.11.0/umd/popper.min.js"
        integrity="sha384-b/U6ypiBEHpOf/4+1nzFpr53nxSS+GLCkfwBdFNTxtclqqenISfwAzpKaMNFNmj4"
        crossorigin="anonymous"></script>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-beta/js/bootstrap.min.js"
        integrity="sha384-h0AbiXch4ZDo7tp9hKZ4TsHbi047NrKGLO3SEJAg45jXxnGIfYzk4Si90RDIqNm1"
        crossorigin="anonymous"></script>
<script src="https://code.highcharts.com/highcharts.js"></script>
<script src="/public/js/jquery.mCustomScrollbar.concat.min.js"></script>
<script src="/public/js/moment.min.js"></script>
<script>
    (function ($) {
        $(window).on("load", function () {
            $(".main-content").mCustomScrollbar({
                theme: 'dark'
            });
        });
    })(jQuery);

    let chartData = {
        last24Hours: {
            dates: [<% for(let i=0; i < data.dayHour.length; i++){ %>"<%= data.dayHour[i].dataValues.time %>",<% } %>],
            temp: [<% for(let i=0; i < data.dayHour.length; i++){ %><%= data.dayHour[i].dataValues.avgtemp %>,<% } %>]
        },
        weekHr: {
            dates: [<% for(let i=0; i < data.weekHr.length; i++){ %>"<%= data.weekHr[i].dataValues.time %>",<% } %>],
            temp: [<% for(let i=0; i < data.weekHr.length; i++){ %><%= data.weekHr[i].dataValues.avgtemp %>,<% } %>]
        },
        week: {
            dates: [<% for(let i=0; i < data.week.length; i++){ %>"<%= data.week[i].dataValues.day %>",<% } %>],
            temp: [<% for(let i=0; i < data.week.length; i++){ %><%= data.week[i].dataValues.avgtemp %>,<% } %>]
        },
        year: {
            dates: [<% for(let i=0; i < data.year.length; i++){ %>"<%= data.year[i].dataValues.month %>",<% } %>],
            temp: [<% for(let i=0; i < data.year.length; i++){ %><%= data.year[i].dataValues.avgtemp %>,<% } %>]
        }
    }
</script>
<script src="/public/js/charts.min.js"></script>